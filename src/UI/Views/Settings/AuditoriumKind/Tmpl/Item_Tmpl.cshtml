@using (var template = Html.Incoding().Template<GetAuditoriumKindsQuery.Response>())
{
    using (var each = template.ForEach())
    {
        @using (Html.When(JqueryBind.Click)
                    .Ajax(Url.Dispatcher().Query<AddOrEditAuditoriumKindCommand.AsQuery>(new { Id = each.For(r => r.Id) }).AsView("~/Views/Settings/AuditoriumKind/AddOrEdit.cshtml"))
                    .OnSuccess(dsl => dsl.WithId(GlobalSelectors.EditAreaId).Insert.Html())
                    .AsHtmlAttributes(new
                    {
                        @class = "d-flex shadow gap-2 rounded py-1 px-2 m-1 shadow-hover cursor-pointer",
                        filter = each.For(r => r.Kind),
                        title = each.For(r => r.Kind)
                    })
                    .ToBeginTag(HtmlTag.Div))
        {
            <div class="my-2">
                @each.For(r => r.Kind)
            </div>
        }
    }
}
