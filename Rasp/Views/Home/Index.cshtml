@using Incoding.Web.MvcContrib
@using Domain
@(Html.When(JqueryBind.InitIncoding)
      .Ajax("/Dispatcher/Query?incType=Inc")
      .OnSuccess(dsl => dsl.Self().Insert.Use(Selector.Result).WithTemplateByView("~/Views/Home/Item_Tmpl.cshtml").Html())
      .AsHtmlAttributes()
      .ToDiv())